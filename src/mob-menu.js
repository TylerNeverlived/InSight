// 'use strict';

// const mobMenuWindow = document.querySelector('.mob-menu');
// const overlay = document.querySelector('.backdrop');
// const btnCloseMobMenuWindow = document.querySelector('.close-mob-btn');
// const btnsShowMobMenuWindow = document.querySelectorAll('.menu-open-btn');
// console.log(btnsShowMobMenuWindow);

// const showMobMenuWindow = function () {
//   mobMenuWindow.classList.add('is-open');
//   //  modalWindow.style.display = 'block';
//   overlay.classList.add('is-open');
// };

// const closeMobMenuWindow = function () {
//   mobMenuWindow.classList.remove('is-open');
//   overlay.classList.remove('is-open');
// };

// for (let i = 0; i < btnsShowMobMenuWindow.length; i++) {
//   btnsShowMobMenuWindow[i].addEventListener('click', showMobMenuWindow);
// }

// btnCloseMobMenuWindow.addEventListener('click', closeMobMenuWindow);

// overlay.addEventListener('click', closeMobMenuWindow);

// document.addEventListener('keydown', function (e) {
//   if (e.key === 'Escape' && !mobMenuWindow.classList.remove('is-open')) {
//     closeMobMenuWindow();
//   }
// });
